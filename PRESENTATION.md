# 📖 عرض تقديمي - كتاب الوصفات
## مدة العرض: 15 دقيقة

---

## 1. نظرة عامة (2 دقيقة)

### ما هو التطبيق؟
- تطبيق Flutter لإدارة الوصفات
- يعمل على الويب والجوال
- واجهة عربية كاملة

### الميزات الرئيسية:
✅ إدارة الوصفات (إضافة، تعديل، حذف)  
✅ المفضلة  
✅ الملاحظات  
✅ الوضع الداكن/الفاتح  
✅ المصادقة المحلية  

---

## 2. التقنيات المستخدمة (3 دقائق)

### Framework & Language
- **Flutter** - Framework متعدد المنصات
- **Dart** - لغة البرمجة

### State Management
- **Provider** - لإدارة حالة التطبيق
- **ChangeNotifier** - للخدمات

### Navigation
- **go_router** - للتنقل بين الشاشات
- **StatefulShellRoute** - للتنقل مع Bottom Navigation

### Storage
- **SharedPreferences** - لتخزين:
  - بيانات المستخدمين
  - الوصفات
  - الملاحظات
  - إعدادات الثيم

---

## 3. البنية المعمارية (3 دقائق)

### هيكل المشروع:
```
lib/
├── main.dart              # نقطة البداية
├── core/                  # المكونات الأساسية
│   └── services/         # الخدمات (Auth, Theme, Database)
├── features/             # الميزات
│   ├── auth/            # المصادقة
│   ├── recipes/         # الوصفات
│   ├── favorites/       # المفضلة
│   ├── notes/           # الملاحظات
│   ├── dashboard/       # لوحة التحكم
│   └── account/         # الحساب
└── shared/              # المكونات المشتركة
```

### الملفات الرئيسية:
1. **main.dart** - إعداد Provider و Router
2. **auth_service.dart** - إدارة المصادقة
3. **recipe_service.dart** - إدارة الوصفات
4. **database_service.dart** - إدارة الملاحظات
5. **theme_service.dart** - إدارة الثيم

---

## 4. الميزات التفصيلية (4 دقائق)

### أ) المصادقة (Authentication)
- **الملفات:** `auth_service.dart`, `login_screen.dart`, `register_screen.dart`
- **التخزين:** SharedPreferences
- **الميزات:**
  - تسجيل الدخول
  - إنشاء حساب (تسجيل دخول تلقائي)
  - حفظ حالة تسجيل الدخول

### ب) الوصفات (Recipes)
- **الملفات:** `recipe_service.dart`, `recipe_list_screen.dart`, `add_recipe_screen.dart`
- **التخزين:** SharedPreferences (JSON)
- **الميزات:**
  - CRUD كامل (Create, Read, Update, Delete)
  - الفلترة حسب الفئة
  - المفضلة
  - عرض التفاصيل

### ج) الملاحظات (Notes)
- **الملفات:** `database_service.dart`, `notes_list_screen.dart`
- **التخزين:** SharedPreferences (تم تحويله من SQLite)
- **الميزات:**
  - إضافة ملاحظات
  - عرض الملاحظات
  - حذف بالتمرير (Swipe to delete)

### د) الثيم (Theme)
- **الملفات:** `theme_service.dart`
- **التخزين:** SharedPreferences
- **الميزات:**
  - التبديل بين الوضع الداكن/الفاتح
  - حفظ التفضيل

---

## 5. إدارة الحالة (2 دقيقة)

### Provider Pattern:
```dart
MultiProvider(
  providers: [
    ChangeNotifierProvider.value(value: AuthService.instance),
    ChangeNotifierProvider.value(value: ThemeService.instance),
  ],
  child: MaterialApp.router(...)
)
```

### استخدام Provider:
```dart
// في الشاشات
final authService = Provider.of<AuthService>(context);
final themeService = Provider.of<ThemeService>(context);

// أو مع Consumer
Consumer<ThemeService>(
  builder: (context, themeService, child) {
    return Widget(...);
  },
)
```

---

## 6. التخزين المحلي (1 دقيقة)

### SharedPreferences Keys:
- `auth_users_json` - بيانات المستخدمين
- `auth_is_logged_in` - حالة تسجيل الدخول
- `auth_email` - البريد الإلكتروني
- `recipes` - قائمة الوصفات
- `app_theme_mode` - الوضع الداكن/الفاتح
- `notes_list` - قائمة الملاحظات

---

## 7. نقاط القوة (1 دقيقة)

✅ **يعمل على جميع المنصات** (Web, Android, iOS)  
✅ **واجهة عربية كاملة** (RTL Support)  
✅ **Material Design 3** - تصميم حديث  
✅ **الوضع الداكن/الفاتح**  
✅ **معالجة الأخطاء** شاملة  
✅ **كود منظم** وواضح  

---

## 8. كيفية التشغيل (30 ثانية)

```bash
# تثبيت الحزم
flutter pub get

# تشغيل على Edge
flutter run -d edge

# تشغيل على Android
flutter run -d android
```

---

## 📊 ملخص سريع

| الميزة | التقنية | الملفات |
|--------|---------|---------|
| المصادقة | SharedPreferences | auth_service.dart |
| الوصفات | SharedPreferences | recipe_service.dart |
| الملاحظات | SharedPreferences | database_service.dart |
| الثيم | SharedPreferences | theme_service.dart |
| State Management | Provider | main.dart |
| Navigation | go_router | main.dart |

---

## 🎯 الخلاصة

تطبيق Flutter متكامل لإدارة الوصفات مع:
- واجهة عربية حديثة
- إدارة حالة باستخدام Provider
- تخزين محلي باستخدام SharedPreferences
- يعمل على جميع المنصات

**جاهز للاستخدام والتطوير! 🚀**

